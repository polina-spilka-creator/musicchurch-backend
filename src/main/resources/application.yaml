spring:
  application:
    name: music-church
  datasource:
    url: jdbc:postgresql://localhost:5432/musicchurch
    username: musicchurch
    password: musicchurch
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.boot: DEBUG